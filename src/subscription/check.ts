import type { CraftProfile } from '../tui/components/craftAuth/CraftCallbackStep';

export async function checkSubscription(profile: CraftProfile): Promise<string | null> {
  void profile;
  return null;
}
